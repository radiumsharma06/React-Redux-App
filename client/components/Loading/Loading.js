import React from 'react';
import { Dimmer, Loader, Image, Segment } from 'semantic-ui-react';

const Loading = () => <Loader active inline='centered'> Fetching Your details... </Loader>;

export default Loading;
